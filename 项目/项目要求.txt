《开心辞典》

1. C/S架构  支持多个客户端同时访问，
服务器：多线程、多进程、多路复用
2. socket通信，tcp  信息保存用数据库
3. 功能模块
   登录、注册、查找单词、查找历史记录、修改个人信息
   服务器端：查看信息
   
   扩展：聊天
   
   服务器功能：
       接收解析客户端发送的请求，
	   数据库的操作
   客户端功能：
       通过UI界面，根据用户输入的信息，封装成命令，向服务器发送请求，
	   接收解析服务器返回的信息
4. Makefile

5. 数据库表格
    单词==》数据库  
    用户信息
    历史记录
        name  word 

5. 备份，
   打印信息：
        定位出错的位置，
		#define _DEBUG_
		printf("%s() %d\n",__func__ , __LINE__);  
		#endif		
   先写框架，功能模块，先封装函数，空在那，框架没有问题了，再去实现具体函数
   
   #if 0
   #endif
   
   func()
   {
 
   
   
   
   
   
   
   
     #if 1   
   
   
   
    #endif  
   }
 
   